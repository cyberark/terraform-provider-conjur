version: '3'
services:
  conjur-server:
    image: registry.tld/conjur-appliance:transition.to.openssl3
    security_opt:
      - seccomp:unconfined # --security-opt seccomp=unconfined
    hostname: conjur
    ports:
      - "443"

