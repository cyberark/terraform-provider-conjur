# Loaded to: data
# conjur policy load -f authn-cert-secrets.yml -b data
- !policy
  id: cert-apps
  body:
  - &variables
    - !variable database/password

  - !permit
    role: !group
    privilege: [ read, execute ]
    resource: *variables
