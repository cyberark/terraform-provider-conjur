#conjur policy load -f authn-host.yml -b data
- !policy
  id: my-service-apps
  body:
      - !group

      - &hosts
        - !host
          id: example_user
          annotations:
              authn-jwt/my-service/user_id: 12345
  
      - !grant
        role: !group
        members: *hosts