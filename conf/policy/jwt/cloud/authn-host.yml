#conjur policy load -f authn-host.yml -b data
- !policy
  id: github-apps
  body:
      - !group

      - &hosts
        - !host
          id: example_user
          annotations:
              authn-jwt/github/user_id: 12345
  
      - !grant
        role: !group
        members: *hosts